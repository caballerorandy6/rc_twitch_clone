import { StreamCard } from "./StreamCard";

export function StreamGrid() {
  return (
    <div>
      {/*
        Grid responsivo de StreamCards
        - CSS Grid con múltiples columnas
        - Gap entre tarjetas
        - Responsive: 1-4 columnas según viewport
      */}
    </div>
  );
}
