// Tipos específicos de la API de Twitch

export type TwitchStream = {
  // Estructura de respuesta de Twitch API
  // Según documentación oficial
};

export type TwitchUser = {
  // Estructura de usuario de Twitch
};

export type TwitchCategory = {
  // Estructura de categoría/juego de Twitch
};

export type TwitchAuthResponse = {
  // Respuesta de OAuth
  // access_token, refresh_token, expires_in, etc
};
