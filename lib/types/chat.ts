export type ChatMessage = {
  // Estructura de datos para un mensaje de chat
  // id, user, message, timestamp, badges, color, etc
};

export type ChatUser = {
  // Usuario en el chat
  // username, color, badges, etc
};
